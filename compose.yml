name: 'srinil-shop'

services:
  postgres:
    container_name: 'srinil-shop-postgres'
    image: 'docker.io/postgres:17-alpine'
    environment:
      - POSTGRES_USER=${DB_USERNAME}
      - POSTGRES_PASSWORD=${DB_PASSWORD}
      - POSTGRES_DB=${DB_DATABASE}
    ports:
      - '5432:5432'
    restart: 'always'
    volumes:
      - pgdata:/var/lib/postgresql/data
    networks:
      - srinil-shop-network
    healthcheck:
      test: ['CMD-SHELL', 'pg_isready -U ${DB_USERNAME} -h localhost']
      interval: 5s
      timeout: 5s
      retries: 10

  mailpit:
    container_name: 'srinil-shop-mailpit'
    image: 'docker.io/axllent/mailpit:latest'
    ports:
      - '1025:1025'
      - '8025:8025'
    restart: 'always'
    networks:
      - srinil-shop-network

volumes:
  pgdata:
    name: 'srinil-shop-pgdata'

networks:
  srinil-shop-network:
    name: 'srinil-shop-network'
